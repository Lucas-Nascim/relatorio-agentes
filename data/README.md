Este diretório contém arquivos de dados de exemplo para o app.

- `Base_DBM.csv`: arquivo de exemplo com colunas mínimas usadas pelo app.

Se desejar, substitua `Base_DBM.csv` por `Base_DBM.xlsx` (mesma estrutura, sheet `dados`).
